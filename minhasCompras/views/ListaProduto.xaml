<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="minhasCompras.Views.ListaProduto"
             >
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Somar" Clicked="ToolbarItem_Clicked_1"/>
        <ToolbarItem Text="Adicionar" Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>

    <VerticalStackLayout>
        
        <SearchBar x:Name="txt_search"
                   Placeholder="Busca de produtos"
                   TextChanged="txt_search_TextChanged"/>
        <ListView x:Name="list_produtos">
            <ListView.Header>
                <Grid ColumnDefinitions="*, *, *, *, *, *">
                    <Label Grid.Column="0" Text="ID" HorizontalOptions="Center"/>
                    <Label Grid.Column="1" Text="Descrição" HorizontalOptions="Center"/>
                    <Label Grid.Column="2" Text="Preço" HorizontalOptions="Center"/>
                    <Label Grid.Column="3" Text="Quantidade" HorizontalOptions="Center"/>
                    <Label Grid.Column="4" Text="Total" HorizontalOptions="Center"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.ContextActions>
                            <MenuItem Text="Remover" Clicked="MenuItem_Clicked" CommandParameter="{Binding Id}"/>
                        </ViewCell.ContextActions>
                        <Grid ColumnDefinitions="*, *, *, *, *, *">
                            <Label Grid.Column="0" Text="{Binding Id}" HorizontalOptions="Center"/>
                            <Label Grid.Column="1" Text="{Binding Descricao}" HorizontalOptions="Center"/>
                            <Label Grid.Column="2" Text="{Binding Preco}" HorizontalOptions="Center"/>
                            <Label Grid.Column="3" Text="{Binding Quantidade}" HorizontalOptions="Center"/>
                            <Label Grid.Column="4" Text="{Binding Total, StringFormat='{0:C2}'}" HorizontalOptions="Center"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
    
   
</ContentPage>